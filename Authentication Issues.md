#auth

If the user does not have the authenticator with their archkey credentials on their device then, in entra admin, under auth methods, add a new one that is the temp acces pass and use that to set up their authenticator